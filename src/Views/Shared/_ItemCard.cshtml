
@model ItemsDisplay.Models.CharacterAttributeViewModel

@* <div class="col">
	<div class="card h-100">
		<img src="@Model.Characters.ImgURL" class="card-img-top" alt="Image not found" loading="lazy">
		<div class="card-body">
			<h5 class="card-title">
				<a asp-controller="Character">					
					@Model.Characters.Name									
				</a>
			</h5>
			<p class="card-text">@Model.Characters.Description</p>
		</div>
		<div class="card-footer user-select-none">
			<a href='@Url.Action("Edit")' class="btn btn-primary">Edit</a>
			<a href='@Url.Action("Edit")' class="btn btn-primary">Delete</a>
		</div>
	</div>
</div> *@
